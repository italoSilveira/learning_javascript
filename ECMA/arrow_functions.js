const square = function(a) {
    return a * a;
}

const square = (a) => {
    return a * a;
}

const square = (a) => a * a;
const square = a => a * a;

//Can be use in array functions like
[1,2,3,4].map(a => a * a);